// 배열 = 여러 개의 값을 순차적으로 담을 수 있는 자료형

// 1. 배열 생성 방법
let arr1 = new Array(); // 배열 생성자
let arr2 = []; // 배열 리터럴 (대부분 사용)

// 2. 배열 요소 접근 방법
// 인덱스로 접근 가능함 (0부터 시작)
let arr3 = [1, 2, 3, 4, 5, 6];
let item1 = arr3[0];
let item2 = arr3[1];

// 수정 가능
arr3[0] = 100;
