export const add = (a, b) => {
  return a + b;
};

export const sub = (a, b) => {
  return a - b;
};

// module.exports = {
//   add,
//   sub,
// };

const multiply = (a, b) => {
  return a * b;
};

export default multiply;
